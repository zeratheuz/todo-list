import { createButtonAddTask } from './createButtonAddTask.js'

export function createTaskForm(divContent) {
  const test = document.createElement("div")
  test.textContent = "TESTING..."
  divContent.appendChild(test)
}